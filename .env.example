# Persistence — optional locally (falls back to in-memory if unset).
DATABASE_URL=

# Stripe — checkout & webhook return 501 until these are set.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_BASE_URL=http://localhost:3000
